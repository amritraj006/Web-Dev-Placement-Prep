// const arr = [1,2,3,4,5,6,2,3];
// arr.push(2); //Re
// arr.pop();

// arr.unshift(2); //Add element in the beginning
// arr.shift(); //Remove "" " "        ""

// console.log(arr.indexOf(2)); //Return first index of matching element
// console.log(arr.lastIndexOf(2)); //Return last index of matching element

// console.log(arr.includes(20)); //Check weather the element exists;

// const str = arr.join(','); //Converts array into string
// console.log(str);

// const arr2 = str.split(", "); //converts a string into a array
// console.log(arr2);

// const filteredArray = arr.filter(num => num%2===0); //Filter out elements based on the given condition and creates a new array
// console.log(filteredArray);

// const get = arr.find(num => num >=6); //Find first matching element
// console.log(get);

// console.log(arr.findIndex(num => num === 3)); //Find first index of matching element
// console.log(arr.findLastIndex(num => num === 3)); //Find last index of mcatching element

// console.log(arr.every(n => n > 2)); //Returns true when every elements satisfy the condition
// console.log(arr.some(n => n < 4)) // Returns true when atleast when element one element satisfy the condition

